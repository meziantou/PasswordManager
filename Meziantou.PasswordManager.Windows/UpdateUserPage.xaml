<UserControl x:Class="Meziantou.PasswordManager.Windows.UpdateUserPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             FocusManager.FocusedElement="{Binding ElementName=UpdateButton}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="We need to update your account before you can continue using PasswordManager. This won't be long." />
                <TextBlock Grid.Row="1" Text="You may need to enter you master key during the process" />
                <Button x:Name="UpdateButton" Grid.Row="2" IsDefault="True" Click="Button_Click">Update</Button>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
